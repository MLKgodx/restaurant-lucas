name: Tests Addition

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Installer les dépendances
        run: npm install

      - name: test addition
        run: npm test