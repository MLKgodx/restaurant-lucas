name: Ouverture du Restaurant

# Le workflow se déclenche à chaque push sur la branche main
on:
  push:
    branches:
      - main

jobs:
  ouverture:
    # Définir l'environnement d'exécution
    runs-on: ubuntu-latest
    
    steps:
      # Étape simple qui annonce l'ouverture
      - name: Annoncer l'ouverture
        run: echo "🍽️ Le restaurant est ouvert !"